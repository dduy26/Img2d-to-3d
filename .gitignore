# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual Environment
.venv/
venv/
env/

# VS Code
.vscode/

# Logs
*.log

# Environment variables
.env

# Build
build/
dist/

# OS
.DS_Store
Thumbs.db

# Python cache
.pytest_cache/
.mypy_cache/

# Node
node_modules/

# Output dữ liệu sinh ra
data/output/*
!data/output/.gitkeep

# ============================================================================
# TripoSR — Mã nguồn bên thứ 3 (clone riêng theo hướng dẫn README)
# Không push vào repo vì nặng (~2GB model weights) và có .git riêng
# ============================================================================
notebook/backend/tsr/

# Model weights & checkpoints (tự tải từ HuggingFace)
*.ckpt
*.safetensors
*.bin
*.pt
*.pth
*.onnx

# Temp uploads & generated outputs (dữ liệu runtime)
temp_uploads/
outputs/
notebook/backend/temp_uploads/
notebook/backend/outputs/

# ============================================================================
# Dữ liệu tải về của riêng từng máy — KHÔNG push (nặng, và mỗi người tải bản khác)
#   data/01/     : 1 bộ DX.GL Objaverse-1K vừa giải nén (196 ảnh 1024x1024 + mask
#                  + depth + poses) = ~121 MB
#   data/images/ : bản sao rời 196 ảnh đó = ~28 MB
#   data/objaverse/ : ảnh do notebook Cell 2 sinh ra (cũng tự tải lại được)
# Muốn đưa vào repo thì nén lại rồi upload lên HF/Drive, đừng commit thẳng.
# ============================================================================
data/01/
data/images/
data/objaverse/